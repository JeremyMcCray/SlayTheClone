package mapNodes;

import models.Battlefield;
import models.Player;
import utilities.BattlefieldManager;

public class BattleNode extends AbstractNode{
    // finish bf manager
   // BattlefieldManager bfManager = new BattlefieldManager();
    public BattleNode(Player player) {
        super(player);
    }

}
